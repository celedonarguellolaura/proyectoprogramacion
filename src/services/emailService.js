// Modo demo: muestra el código en pantalla.
// Para envío real instala @emailjs/browser, configura las claves y descomenta el bloque de abajo.

export async function sendVerificationEmail(_toEmail, _userName, _code) {
  return { ok: false, demo: true }
}
